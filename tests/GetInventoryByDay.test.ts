import { buildApp } from "@build";
import { runTestForDayNumber } from "./fixtures/GetInventoryByDay.fixtures";

describe("GetInventoryByDay testing - ", () => {
  const dependencies = buildApp();

  it("should get inventory of day 1", () => {
    runTestForDayNumber(dependencies, 1);
  });

  it("should get inventory of day 2", () => {
    runTestForDayNumber(dependencies, 2);
  });

  it("should get inventory of day 3", () => {
    runTestForDayNumber(dependencies, 3);
  });

  it("should get inventory of day 4", () => {
    runTestForDayNumber(dependencies, 4);
  });

  it("should get inventory of day 5", () => {
    runTestForDayNumber(dependencies, 5);
  });

  it("should get inventory of day 6", () => {
    runTestForDayNumber(dependencies, 6);
  });

  it("should get inventory of day 7", () => {
    runTestForDayNumber(dependencies, 7);
  });

  it("should get inventory of day 8", () => {
    runTestForDayNumber(dependencies, 8);
  });

  it("should get inventory of day 9", () => {
    runTestForDayNumber(dependencies, 9);
  });

  it("should get inventory of day 10", () => {
    runTestForDayNumber(dependencies, 10);
  });

  it("should get inventory of day 11", () => {
    runTestForDayNumber(dependencies, 11);
  });

  it("should get inventory of day 12", () => {
    runTestForDayNumber(dependencies, 12);
  });

  it("should get inventory of day 13", () => {
    runTestForDayNumber(dependencies, 13);
  });

  it("should get inventory of day 14", () => {
    runTestForDayNumber(dependencies, 14);
  });

  it("should get inventory of day 15", () => {
    runTestForDayNumber(dependencies, 15);
  });

  it("should get inventory of day 16", () => {
    runTestForDayNumber(dependencies, 16);
  });

  it("should get inventory of day 17", () => {
    runTestForDayNumber(dependencies, 17);
  });

  it("should get inventory of day 18", () => {
    runTestForDayNumber(dependencies, 18);
  });

  it("should get inventory of day 19", () => {
    runTestForDayNumber(dependencies, 19);
  });

  it("should get inventory of day 20", () => {
    runTestForDayNumber(dependencies, 20);
  });

  it("should get inventory of day 21", () => {
    runTestForDayNumber(dependencies, 21);
  });

  it("should get inventory of day 22", () => {
    runTestForDayNumber(dependencies, 22);
  });

  it("should get inventory of day 23", () => {
    runTestForDayNumber(dependencies, 23);
  });

  it("should get inventory of day 24", () => {
    runTestForDayNumber(dependencies, 24);
  });

  it("should get inventory of day 25", () => {
    runTestForDayNumber(dependencies, 25);
  });

  it("should get inventory of day 26", () => {
    runTestForDayNumber(dependencies, 26);
  });

  it("should get inventory of day 27", () => {
    runTestForDayNumber(dependencies, 27);
  });

  it("should get inventory of day 28", () => {
    runTestForDayNumber(dependencies, 28);
  });

  it("should get inventory of day 29", () => {
    runTestForDayNumber(dependencies, 29);
  });

  it("should get inventory of day 30", () => {
    runTestForDayNumber(dependencies, 30);
  });
});
